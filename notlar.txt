== ve equals() farklý. == memory adresine bakar. split methodu kullandýktan sonra equals kullan(1)

str = str.replaceAll("\\s", "");                                                               (2)
str = str.replaceAll("[)][(]", ")*(");
str = str.replaceAll("([0-9])[(]", "$1*(");
str = str.replaceAll("[)]([0-9])", ")*$1");

boolean bir array oluþturduðunda içindeki tüm deðerler default false olur en azýndan javada öyle oluyor (3)

String s=Integer.toBinaryString(4); //binaryString oluþturma
int num=Integer.parseInt(sb.toString(),2);  yandaki 2 binary sistemde çevir demek.  (4)

int n= 'b' - 'a'  //chardan charý çýkarýrsan veya toplarsan integer döner. (5)

Collections.sort(list,Collections.reverseOrder());  //hackerrankta reversort
Collections.sort(list)  //hackerrankta sort ,list or array

kk - hours - range (1 to 24) - hours in 24 hours format
HH - hours - range (0 to 23) - hours in 24 hours format
hh - hours - range (1 to 12) - hours in 12 hours format with AM/PM

Arrays.asList(maxRecord,minRecord);   //list oluþturma 

Character.toLowerCase(ch) and Character.isUpperCase(ch)







